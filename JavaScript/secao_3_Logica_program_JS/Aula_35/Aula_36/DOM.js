/**
 *  o window é tipo o pai e a mãe do documento pelo browser 
 *  document é onde o HTML fica 
 *  o document é o topo da arvora do DOM considerando isso 
 *  HTML é child de document, e body/ head é child de HTML 
 *  Sendo do pai pro filho
 *  document > html > head > body 
 *  geralmente,tudo que vem dentro de uma tag como, body, section, div,
 *  são respectivas filhas das tags acima 
 */